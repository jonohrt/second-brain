---
phase: 04-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/SecondBrain/Models/APIModels.swift
  - ios/SecondBrain/Services/SpeechService.swift
  - ios/SecondBrain/ViewModels/AppViewModel.swift
  - ios/SecondBrain/Views/ContentView.swift
  - ios/SecondBrainTests/APIClientTests.swift
  - ios/SecondBrainTests/SpeechServiceTests.swift
autonomous: false
requirements:
  - RESP-03
  - RESP-04

must_haves:
  truths:
    - "User can toggle TTS on and the app reads the response aloud after receiving an answer"
    - "Long responses are split into sentences so TTS does not cut off"
    - "TTS stops when user starts a new recording or sends a new question"
    - "When the answer was informed by vault notes, source note titles appear below the answer"
    - "When only web sources exist, no vault sources section is shown"
  artifacts:
    - path: "ios/SecondBrain/Services/SpeechService.swift"
      provides: "AVSpeechSynthesizer wrapper with sentence splitting"
      min_lines: 40
    - path: "ios/SecondBrain/Models/APIModels.swift"
      provides: "AskSource with path field for vault sources"
      contains: "let path: String?"
    - path: "ios/SecondBrain/ViewModels/AppViewModel.swift"
      provides: "TTS toggle state, currentSources array, speechService integration"
      contains: "isTTSEnabled"
    - path: "ios/SecondBrain/Views/ContentView.swift"
      provides: "TTS toggle button and vault sources display"
      contains: "Sources"
    - path: "ios/SecondBrainTests/SpeechServiceTests.swift"
      provides: "Unit tests for sentence splitting logic"
      min_lines: 20
    - path: "ios/SecondBrainTests/APIClientTests.swift"
      provides: "Fixed mock with proper AskSource objects, path field test"
      contains: "path"
  key_links:
    - from: "ios/SecondBrain/ViewModels/AppViewModel.swift"
      to: "ios/SecondBrain/Services/SpeechService.swift"
      via: "speechService.speak() called after API response when isTTSEnabled"
      pattern: "speechService\\.speak"
    - from: "ios/SecondBrain/ViewModels/AppViewModel.swift"
      to: "ios/SecondBrain/Models/APIModels.swift"
      via: "currentSources populated from response.sources"
      pattern: "currentSources.*response\\.sources"
    - from: "ios/SecondBrain/Views/ContentView.swift"
      to: "ios/SecondBrain/ViewModels/AppViewModel.swift"
      via: "UI reads vaultSources and isTTSEnabled from viewModel"
      pattern: "viewModel\\.vaultSources"
---

<objective>
Add text-to-speech readback and source attribution to the iOS app.

Purpose: Users can listen to answers hands-free and see which vault notes informed the response -- completing the v1 feature set.
Output: SpeechService, updated AppViewModel/ContentView with TTS toggle and sources display, updated tests.
</objective>

<execution_context>
@/Users/jimi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhancements/04-RESEARCH.md
@.planning/phases/04-enhancements/04-VALIDATION.md

@ios/SecondBrain/Models/APIModels.swift
@ios/SecondBrain/Services/APIClient.swift
@ios/SecondBrain/ViewModels/AppViewModel.swift
@ios/SecondBrain/Views/ContentView.swift
@ios/SecondBrainTests/APIClientTests.swift
@ios/project.yml

<interfaces>
<!-- Key types and contracts the executor needs. -->

From ios/SecondBrain/Models/APIModels.swift:
```swift
struct AskSource: Decodable {
    let type: String?
    let url: String?
    let title: String?
    // MISSING: let path: String? -- server sends this for vault sources
}

struct AskResponse: Decodable {
    let answer: String
    let sources: [AskSource]?
    let route: String?
    let model: String?
}
```

From ios/SecondBrain/ViewModels/AppViewModel.swift:
```swift
@Observable @MainActor
class AppViewModel {
    var transcription: String = ""
    var answer: String = ""
    var isRecording: Bool = false
    var isTranscribing: Bool = false
    var isLoading: Bool = false
    var error: String? = nil
    var isWhisperReady: Bool = false
    var setupMessage: String? = nil

    private let apiClient: APIClient
    private let recorder: AudioRecorder
    private let transcriber: TranscriptionService

    func sendQuestion() async { ... }  // Currently discards response.sources
    func startRecording() { ... }
}
```

Server /ask response shape (from src/services/ask-pipeline.ts):
```json
{
  "answer": "...",
  "sources": [
    { "type": "vault", "path": "notes/topic.md", "title": "Topic Note", "similarity": 0.82 },
    { "type": "web", "url": "https://...", "title": "Web Result" }
  ],
  "route": "brain",
  "model": "gemma3:12b"
}
```

From ios/SecondBrainTests/APIClientTests.swift (line 63 -- BROKEN mock):
```swift
// Current: "sources":["note.md"]  -- string array, wrong
// Should be: "sources":[{"type":"vault","path":"notes/note.md","title":"My Note"}]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpeechService, AskSource path field, and tests</name>
  <files>
    ios/SecondBrain/Services/SpeechService.swift,
    ios/SecondBrain/Models/APIModels.swift,
    ios/SecondBrainTests/SpeechServiceTests.swift,
    ios/SecondBrainTests/APIClientTests.swift
  </files>
  <behavior>
    - SpeechServiceTests: splitIntoSentences("Hello world. How are you?") returns ["Hello world. ", "How are you?"] (two sentences)
    - SpeechServiceTests: splitIntoSentences("") returns [] (empty input)
    - SpeechServiceTests: splitIntoSentences("One sentence") returns ["One sentence"] (no period)
    - SpeechServiceTests: splitIntoSentences("Dr. Smith went home. He rested.") returns 2 sentences (abbreviation handled)
    - APIClientTests: testAskWithVaultSourcesDecodesPathField -- mock returns proper source objects with type/path/title, asserts path is decoded
    - APIClientTests: fix existing testAskWithValidResponseDecodesCorrectly mock to use proper source objects instead of string array
  </behavior>
  <action>
    1. Add `let path: String?` to `AskSource` in APIModels.swift. Add `Identifiable` conformance with `var id: String { path ?? url ?? title ?? UUID().uuidString }`.

    2. Create `ios/SecondBrain/Services/SpeechService.swift`:
       - `final class SpeechService: NSObject, AVSpeechSynthesizerDelegate`
       - Private `AVSpeechSynthesizer` instance, reused across calls
       - `private(set) var isSpeaking = false`
       - `var onFinished: (() -> Void)?` callback
       - `func speak(_ text: String)` -- calls stop() first, splits text via `splitIntoSentences`, creates one `AVSpeechUtterance` per sentence with `AVSpeechSynthesisVoice(language: "en-US")` and default rate, queues all on synthesizer
       - `func stop()` -- calls `synthesizer.stopSpeaking(at: .immediate)`, sets isSpeaking = false
       - `func splitIntoSentences(_ text: String) -> [String]` -- uses `NLTokenizer(unit: .sentence)`, filters empty strings. Make this `internal` (not private) so tests can call it directly.
       - Delegate `speechSynthesizer(_:didFinish:)` -- check `synthesizer.isSpeaking`, only set `isSpeaking = false` and call `onFinished?()` when synthesizer is done (iOS 17 bug workaround)
       - Import AVFoundation and NaturalLanguage

    3. Create `ios/SecondBrainTests/SpeechServiceTests.swift`:
       - Test sentence splitting with multiple sentences, single sentence, empty string, abbreviation handling
       - Instantiate SpeechService and call splitIntoSentences directly

    4. Fix `ios/SecondBrainTests/APIClientTests.swift`:
       - In `testAskWithValidResponseDecodesCorrectly`, change the mock JSON from `"sources":["note.md"]` to `"sources":[{"type":"vault","path":"notes/note.md","title":"My Note","similarity":0.82}]`
       - Update the assertion from `XCTAssertEqual(result.sources, ["note.md"])` to check `result.sources?.first?.type == "vault"` and `result.sources?.first?.path == "notes/note.md"` and `result.sources?.first?.title == "My Note"`
       - Add new test `testAskWithWebSourceDecodesCorrectly` with mock returning `{"type":"web","url":"https://example.com","title":"Example"}` source, assert url field decoded

    5. Regenerate Xcode project: `cd ios && xcodegen generate`
  </action>
  <verify>
    <automated>cd /Users/jimi/Code/second-brain/ios && xcodegen generate && xcodebuild test -project SecondBrain.xcodeproj -scheme SecondBrain -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SecondBrainTests/SpeechServiceTests -only-testing SecondBrainTests/APIClientTests 2>&1 | tail -20</automated>
  </verify>
  <done>SpeechService created with sentence splitting, AskSource has path field with Identifiable conformance, all SpeechServiceTests and APIClientTests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire TTS and source attribution into ViewModel and UI</name>
  <files>
    ios/SecondBrain/ViewModels/AppViewModel.swift,
    ios/SecondBrain/Views/ContentView.swift
  </files>
  <action>
    **AppViewModel.swift changes:**

    1. Add new state properties:
       - `var isTTSEnabled: Bool = false` -- user toggle (no persistence needed for v1, but use @AppStorage in ContentView binding if straightforward)
       - `var currentSources: [AskSource] = []` -- populated from API response

    2. Add computed property:
       - `var vaultSources: [AskSource] { currentSources.filter { $0.type == "vault" } }`

    3. Add private service:
       - `private let speechService = SpeechService()`

    4. Update `sendQuestion()`:
       - Call `speechService.stop()` before the API call (stop any in-progress TTS)
       - After `answer = response.answer`, add `currentSources = response.sources ?? []`
       - After setting answer, if `isTTSEnabled`, call `speechService.speak(response.answer)`

    5. Update `startRecording()`:
       - Call `speechService.stop()` at the beginning (before guard) to stop TTS when user starts recording

    6. Add `func toggleTTS()`:
       - Flip `isTTSEnabled`
       - If now disabled and speechService.isSpeaking, call `speechService.stop()`
       - If now enabled and answer is not empty, call `speechService.speak(answer)`

    7. Add `var isSpeaking: Bool { speechService.isSpeaking }` for UI state

    **ContentView.swift changes:**

    1. Add TTS toggle button in the header HStack (after "Second Brain" title, before Spacer):
       - After `Spacer()`, add a Button with `speaker.wave.2.fill` (when TTS enabled) or `speaker.slash.fill` (when disabled) SF Symbol
       - Action calls `viewModel.toggleTTS()`
       - Use `.foregroundColor(viewModel.isTTSEnabled ? .blue : .secondary)`

    2. Add source attribution section inside the ScrollView, below the answer Text:
       - Only show when `!viewModel.vaultSources.isEmpty`
       - Divider, then VStack(alignment: .leading, spacing: 4)
       - "Sources" header in `.font(.caption.bold()).foregroundColor(.secondary)`
       - ForEach over `viewModel.vaultSources` using `\.id`
       - Each source: HStack with `doc.text` SF Symbol + source title (fallback to path, fallback to "Unknown")
       - Style: `.font(.caption).foregroundColor(.secondary)`
       - Wrap in `.padding(.horizontal)` to match answer padding

    3. Clear sources on new question: currentSources is already cleared in sendQuestion (set from new response)
  </action>
  <verify>
    <automated>cd /Users/jimi/Code/second-brain/ios && xcodebuild test -project SecondBrain.xcodeproj -scheme SecondBrain -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SecondBrainTests 2>&1 | tail -20</automated>
  </verify>
  <done>TTS toggle visible in header, toggleTTS starts/stops speech, vault sources display below answer text when present, all existing tests still pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device verification of TTS and source attribution</name>
  <files>N/A</files>
  <action>
    Build and run on physical iPhone. Verify TTS audio playback and source attribution display.

    What was built: Text-to-speech readback with sentence splitting and source attribution display in the iOS app.

    How to verify:
    1. Build and run on device: open ios/SecondBrain.xcodeproj, run on iPhone
    2. TTS test: Tap the speaker icon in the header (should turn blue). Ask a question. After the answer appears, verify audio plays through the speaker, reading the answer aloud. Tap the speaker icon again -- speech should stop immediately.
    3. TTS-on-new-question test: With TTS enabled, ask a question. While it is reading aloud, ask another question. Verify the old speech stops and the new answer is read.
    4. TTS-on-record test: With TTS enabled and speech playing, press and hold the record button. Verify speech stops when recording starts.
    5. Source attribution test: Ask a question about something known to be in your vault (e.g., a topic you have notes on). Verify that below the answer, a "Sources" section appears with vault note titles.
    6. No-sources test: Ask a general knowledge question (e.g., "What is the capital of France?"). Verify no vault sources section appears (only web sources, which are not displayed).

    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>Manual device verification -- all 6 checks above pass</verify>
  <done>TTS plays audio on device, stops on toggle/record/new-question, vault sources display below answer for brain-routed queries</done>
</task>

</tasks>

<verification>
- All unit tests pass: `xcodebuild test -project ios/SecondBrain.xcodeproj -scheme SecondBrain -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing SecondBrainTests`
- SpeechService splits sentences correctly (tested via SpeechServiceTests)
- AskSource decodes path field (tested via APIClientTests)
- TTS toggle, speech playback, and source display verified on device
</verification>

<success_criteria>
- RESP-03: User can toggle TTS on, app reads response aloud with sentence splitting, TTS stops on toggle off/new question/new recording
- RESP-04: Vault source titles appear below the answer when the response includes vault sources; no sources section shown for web-only results
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhancements/04-01-SUMMARY.md`
</output>
